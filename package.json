{
  "name": "nex-food-directory",
  "version": "1.0.0",
  "private": true,
  "description": "Care Bears themed food directory for NEX Mall, Serangoon",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site — no build step required'",
    "start": "npx serve ."
  }
}
